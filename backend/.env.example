# Copy this file to .env before running outside the dev container.
# Inside the container, DATABASE_URL is already injected via docker-compose.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medcalc
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
