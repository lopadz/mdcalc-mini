import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack is the default bundler in Next.js 16 dev mode.
  // It reads paths from tsconfig.json automatically, so no extra alias config is needed.
};

export default nextConfig;
